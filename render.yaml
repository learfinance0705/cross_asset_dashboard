services:
  - type: web
    name: cross-asset-site
    runtime: docker
    plan: starter
    autoDeploy: true
    healthCheckPath: /healthz
    envVars:
      - key: ADMIN_TOKEN
        generateValue: true
      - key: BASE_URL
        sync: false
